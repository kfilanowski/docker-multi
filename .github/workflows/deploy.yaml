name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t kfilanowski/react-mutli-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true kfilanowski/react-multi-test npm test

      - run: docker build -t kfilanowski/multi-client ./client
      - run: docker build -t kfilanowski/multi-nginx ./nginx
      - run: docker build -t kfilanowski/multi-server ./server
      - run: docker build -t kfilanowski/multi-worker ./worker

      - run: docker push kfilanowski/multi-client
      - run: docker push kfilanowski/multi-nginx
      - run: docker push kfilanowski/multi-server
      - run: docker push kfilanowski/multi-worker
